## Purpose

<!-- One or two sentences on what this capability IS and what it is held to — the
durable description that outlives this change (Outcomes below is change-scoped;
this is not). Required by `openspec validate`: a promoted spec without it fails,
which is what makes `openspec archive` abort. Carried into a NEW capability spec at
archive time; for a capability that already exists, that spec's own Purpose wins and
this one is ignored. Keep it 50+ characters or `validate --strict` warns. -->

## Outcomes

- **Who:**
- **Job:**
- **Done when:**
- **Not doing:**

## ADDED Requirements

### Requirement:

One plain-language sentence describing what the user experiences.

**Fails until:** (optional — observable check that is false until this requirement is met)

#### Scenario:

- **WHEN**
- **THEN**
