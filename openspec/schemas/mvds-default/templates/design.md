## Context

## Goals / Non-Goals

**Goals:**

**Non-Goals:**

## User flow / IA

## Visual design / Figma

> The proposal (1.0) is generated with the APPROVED Eval Summary (discovery 0.6)
> in context. UI changes require an **as-is + proposed** pair — the `0.0 As is`
> page from discovery plus a `0N.0 Propose:` page per `rules/figma.mdc`.
> Removals count as UI changes. N/A only for genuinely no-UI changes.

| Item                 | Value                                                                          |
| -------------------- | ------------------------------------------------------------------------------ |
| Primary file URL     | MVDS Core or scratch — `https://www.figma.com/design/C20nU0mROzk3Zr0I9BELJF/…`  |
| As-is page / frame   | `0.0 As is` — from discovery (current surface reconstructed from code)         |
| Proposed page / frame| `01.0 Propose: <change-slug>` — changed surface on MVDS primitives (bump per iteration) |
| Libraries / version  | MVDS Core (Tokens / Scales / components) — key or `@beckharrisdesign/mvds@x.y.z` |
| Breakpoints          | S · 480px / L · 1024px (BHD Content Types); Storybook canvas as needed          |
| Status               | draft / iterating / ready for apply                                            |

Map frames to **MVDS** tokens and components (`AGENTS.md`, `src/index.css`) — not hub Tailwind.
No Code Connect row: MVDS does not use Code Connect (`AGENTS.md`).

## Eval Summary → Proposal

> How the proposal answers discovery 0.6 — one row per top issue; call out any
> don't-break item the proposal touches.

| 0.6 item | How the proposal addresses it |
| --- | --- |
| | |

## Eval Delta (1.5)

> Same rubric, same isolation rules as 0.5, run against the proposal. Every
> baseline finding gets a disposition. Informational — a regression is
> highlighted at the checkpoint, never auto-blocking. Iterations re-run only
> this section against the newest `0N.0` page.

| 0.5 finding | Disposition (addressed / deliberately preserved / regressed / new) | Note |
| --- | --- | --- |
| F1 | | |

## Decisions

## Risks / Trade-offs
