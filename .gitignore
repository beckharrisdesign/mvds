# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-lib
dist-site
dist-ssr
*.local
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# The starter is verified with a FRESH resolve every run (scripts/verify-consumer.mjs),
# so its lockfile is deliberately not committed — a stale lock would hide exactly
# the registry/version drift the check exists to catch.
examples/starter/package-lock.json
