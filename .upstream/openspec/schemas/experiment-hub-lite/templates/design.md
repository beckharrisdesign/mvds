## Context

## Goals / Non-Goals

**Goals:**

**Non-Goals:**

## User flow / IA

## Visual design / Figma

> UI changes require a Figma **as-is + proposed** pair (see `openspec/changes/stop-the-leaks/design.md`). N/A only for genuinely no-UI changes.

| Item               | Value                                                                                                 |
| ------------------ | ----------------------------------------------------------------------------------------------------- |
| Primary file URL   |                                                                                                       |
| As-is frame(s)     | (current surface reconstructed from code / captured from running page)                                |
| Proposed frame(s)  | (changed surface on MVDS + `tailwind.config.ts` tokens)                                                |
| Libraries / version| (design-system library + version, e.g. `MVDS Core` / key or `@beckharrisdesign/mvds@0.2.0`)            |
| Code Connect       | (mappings to update? — `figma-code-connect` skill)                                                     |
| Breakpoints       | S · 480px mobile / L · 1024px desktop (BHD Content Types) — see `rules/design-guidelines.mdc` |
| Status            |                                                                                                       |

## Decisions

## Risks / Trade-offs
