# figma-generate-library (placeholder)

Replace this with `SKILL.md` captured from `skill://figma/figma-generate-library/SKILL.md`.

Generates/syncs a Figma design-system library (token collections, light/dark modes,
component libraries) from code. This is the skill the code→Figma sync in
[`docs/SYNC.md`](../../../docs/SYNC.md) invokes — and what the pending Button/Card
component regeneration needs. See [`../README.md`](../README.md) for capture steps.
